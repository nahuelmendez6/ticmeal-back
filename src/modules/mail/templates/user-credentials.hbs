<!DOCTYPE html>
<html>
  <head>
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; }
      .container { padding: 20px; background-color: #f9f9f9; border-radius: 10px; }
      .header { font-size: 20px; font-weight: bold; color: #333; }
      .credentials { margin-top: 15px; background: #fff; padding: 15px; border-radius: 8px; border: 1px solid #eee; }
      .footer { margin-top: 20px; font-size: 12px; color: #666; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">Bienvenido a {{companyName}} 🎉</div>
      <p>Hola <b>{{firstName}}</b>,</p>
      <p>Tu cuenta ha sido creada correctamente. Aquí tienes tus credenciales:</p>
      <div class="credentials">
        <p><b>Usuario:</b> {{username}}</p>
        {{#if password}}
        <p><b>Contraseña:</b> {{password}}</p>
        {{/if}}
        {{#if pin}}
        <p><b>PIN:</b> {{pin}}</p>
        {{/if}}
      </div>
      <p>Por favor, inicia sesión y cambia tu contraseña lo antes posible.</p>
      <div class="footer">
        Gracias,<br/>
        El equipo de {{companyName}}
      </div>
    </div>
  </body>
</html>
